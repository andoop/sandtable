---
description: 查看当前 Sandtable 需求的状态机、任务进度、预演结果与未决问题。
---

读取并遵循 `skills/state-and-memory/SKILL.md`。

执行（只读，不改动）：
1. 列出 `docs/sandtable/features/` 下的需求目录。
2. 对当前（或我指定的）需求，读 `state.md`，汇报：当前 phase、blocked 状态、各任务 status、mental/impl 预演的 runs 与 last 结果、selected_impl。
3. 读 `questions.md`，列出所有"待答复"的阻塞问题。
4. 读 `journal.md` 最近若干条，给出最近发生了什么的简述。
5. 给出"下一步建议动作"（例如：继续澄清 / 写计划 / 预演 / 解决阻塞问题 / 落地）。

用简洁的表格/列表汇报，不要改任何文件。
