---
description: View the current requirement's state machine, autopilot quotas, task progress, and unresolved questions.
---

Show the status of the current Sandtable requirement (read-only); read and follow `skills/state-and-memory/SKILL.md`.

Execute (read-only, do not modify files):
1. List the requirement directories under `docs/sandtable/features/`.
2. For the current requirement (or the one I specify), read `state.md` and report: current `phase`, `blocked` status, each task status, `rehearsals.*` `runs/last`, `autonomy.mode`, `autonomy.min_rounds`, `autonomy.completed_rounds`, `autonomy.last_decision`, and `selected_impl`.
3. Read `questions.md` and list all unresolved blocking questions.
4. Read the most recent `journal.md` entries and summarize what happened recently.
5. Be explicit that `rehearsals.*` are report summaries while `autonomy.completed_rounds` are autopilot quota counters; do not confuse one for the other.
6. Give a "recommended next action" (for example: clarify requirements / write the plan / rehearse / resolve blockers / integrate).

Use a concise table or list in the report. Do not edit any files.
