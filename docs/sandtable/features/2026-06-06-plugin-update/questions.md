# 待开发者澄清 · Questions

## D1 交付形态（状态：待答复）
- 问题：更新机制以什么形态交付？
- 可选项：A. **`UPDATE.md`（AI 驱动，镜像 INSTALL.md 结构）**——读 UPDATE.md 覆盖方法论资产、硬保护 docs/sandtable，处理 zh/en 与各 harness（推荐：与安装同范式，能处理 locale/harness 细节）；B. 一个 `scripts/sandtable-update.sh` 纯脚本覆盖；C. 两者都要（UPDATE.md 主，脚本辅）。
- 开发者答复：
- 已写回：

## D2 版本标记（状态：待答复）
- 问题：是否引入版本号/VERSION 标记（便于检测"需不需要更新"、报告版本差）？
- 可选项：A. v1 **不做**版本号（YAGNI），更新即"覆盖到最新"，不做差量检测（推荐：最简）；B. 引入 `VERSION` 文件 + 更新时比对/报告；C. 用 git commit short-sha 作版本标记。
- 开发者答复：
- 已写回：

## D3 用户改过方法论资产怎么办（状态：待答复）
- 问题：更新覆盖方法论资产时，若用户**自己改过**某资产（如 AGENTS.md / rules），怎么处理？（目标项目不一定是 git 仓）
- 可选项：A. **默认覆盖到最新，但更新前先备份**被覆盖文件到 `*.sandtable-bak`（或带时间戳目录），并在报告中列出"已覆盖且备份"清单，提示用户自行比对找回自定义（推荐：既拿到更新又不丢改）；B. 直接覆盖、不备份、仅警告"会丢自定义，请先 git diff/备份"；C. 检测到改动则跳过该文件并报告（保守，但拿不到该文件更新）。
- 开发者答复：
- 已写回：

## D4 官方更新提示词（状态：待答复）
- 问题：是否像 INSTALL.md 那样提供官方可复制"更新提示词"（指向 UPDATE.md）？
- 可选项：A. 是，提供中/英两条官方更新提示词（指向 UPDATE.md），与安装入口对称（推荐）；B. 不单设提示词，仅在 README/INSTALL 里加"如何更新"一节指向 UPDATE.md。
- 开发者答复：
- 已写回：

## D5 更新时语言如何确定（状态：待答复）
- 问题：更新该用 zh 还是 en locale pack 覆盖，如何确定？
- 可选项：A. 同安装——由用户贴的中/英官方更新提示词正文精确匹配决定（推荐：与安装一致、不猜）；B. **自动探测**已安装语言（读现有 AGENTS.md 标志）再用同语言覆盖；C. A+B：优先按提示词，并校验与现装语言一致，不一致则停下澄清（防误把 zh 装成 en）。
- 开发者答复：
- 已写回：
