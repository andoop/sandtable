# README GitHub 首页重塑 · 改动计划

**目标:** 把 `README.md` 改造成一个更成熟、简洁、AI 友好的 GitHub 首页，在保留上手路径的同时，强化流程图、优势表达、`superpowers` 对比与自举信号。
**架构:** 只修改 `README.md` 的首页叙事与信息层级，不改方法论本体。采用“首屏价值主张 + 核心记忆点 + 试用入口摘要 + 紧随其后的闭环图 + 后续对比/自举/试用展开”的结构重组方式，用现有真实能力支撑对外表达；同时对首页长度、前两屏优先级和对比边界施加硬约束，避免 README 变成长而空的叠加页。
**对应 PRD:** `prd.md`
**预演要求:** 本计划将由头脑预演、红蓝对抗、实现预演子 agent 逐任务推演；自动模式最低配额按 `mental 3x3 / redteam 3x3 / impl 2x2` 执行。

---

## 文件地图
- 修改 `README.md` — 重构 GitHub 首页信息架构与文案，补入流程图、对比和自举信号，同时保留安装/命令入口。
- 修改 `docs/sandtable/features/2026-06-03-readme-homepage-refresh/state.md` — 记录 autopilot 进度。
- 修改 `docs/sandtable/features/2026-06-03-readme-homepage-refresh/journal.md` — 追加侦察、推演、异常、集成与验证结论。
- 创建 `docs/sandtable/features/2026-06-03-readme-homepage-refresh/rehearsals/*.md` — 各轮 mental / redteam / impl 报告。

---

## README 骨架（实现时必须遵守）
1. 标题
2. 1 句副文
3. `<=5` 条核心记忆点
4. 1 条可行动试用入口摘要
5. 闭环图 + 白话说明
6. 1 条极短事实型 `superpowers` 微对比
7. 2 个短锚点（一个指向对比，一个指向安装/Quickstart）
8. `Why Sandtable` / `Why not just let AI code`
9. `Sandtable vs Superpowers`
10. 自举证明
11. `Quickstart`（统一 AI 快速接入；含安装指令 / 接线动作 / 第一条命令）
12. 命令入口
13. 目录结构 / 底线 / License（能压缩就压缩）

- 首屏只允许落入 1-4。
- 5-7 必须紧接首屏出现，并且仍位于前两屏之内；不得回挤进首屏。
- 首屏之后、前两屏之内不允许插入完整对比表、完整 `Quickstart`、完整命令表。
- 实现时不得新增这份骨架之外的首屏元素；若需要新增，只能替换现有骨架中的同级元素。
- 首屏记忆点每条只允许“1 个能力 + 1 个结果”；至少 2 条必须是结果导向句，不得写成纯名词堆叠。
- 闭环图本身或紧邻说明必须附一层白话解释，不能只留下术语节点。
- 首屏微对比只允许 1 条、只允许 1 个已核实事实差异，不得展开成完整对比表。
- 标题下副文、首屏主句、`Why` 段的每一句都必须能就近回指到仓库内可见事实；不允许出现“更/最/真正/不再/保证/更值得试”之类词。

## 旧 Section 处置清单
- 现有开头定义段：**合并到新首屏**，不再保留独立长段解释。
- 现有“核心特性”：**拆分**，其中首屏只保留 `<=5` 条记忆点；其余细节下沉或删除重复项。
- 现有闭环图：**保留但上移** 到首屏试用摘要之后。
- 现有安装 / 接入：**压缩并并入 `Quickstart`**，不再保留一个会与试用锚点竞争的独立“安装 / 接入”主 section。
- 统一默认路径：**固定为“让 AI 读 `INSTALL.md` 并自助安装”**。
- README 不再展开 `Claude Code marketplace` 与 `Cursor` 分栏。
- 手工接线 / 本地试用 / 受保护追加：**下沉到 `INSTALL.md`**，README 只保留统一入口与第一条命令。
- 现有命令表：**保留但压缩**，不抢前两屏。
- 现有目录结构：**压缩保留**，仅保留最有识别度的树形和必要注释。
- 现有“四条不可违背的底线”：**下沉压缩**，不占首页主叙事。
- 任何与新首屏重复解释 Sandtable 是什么、为什么不同的旧段落：**删除或并入新首屏**，不得双写。

## 现有 README 逐段映射表
- 开头标题 + 两段摘要：**改写并并入新首屏** -> 顶部骨架 `1-4`
- `## 核心特性`：**拆分压缩** -> 首屏记忆点 + `Why Sandtable`
- `## 闭环`：**整体上移保留** -> 顶部骨架 `5`
- `## 安装 / 接入`
  - `### Claude Code`：**删除分栏展开** -> 并入统一 `Quickstart`
  - `### Cursor`：**删除分栏展开** -> 并入统一 `Quickstart`
  - `### 让 AI 自助安装（任意 harness）`：**上移为唯一默认路径** -> `Quickstart`
  - `### 手工拷贝（兜底）` / 本地插件（试用）：**下沉移除出 README** -> 保留在 `INSTALL.md`
- `## 用法 · 命令即战术动作`：**压缩保留** -> `命令入口`
- `### 三类推演三位一体`：**部分并入** -> 首屏记忆点 + `Why Sandtable`
- `## 目录结构`：**压缩保留** -> `目录结构`
- `## 四条不可违背的底线`：**下沉压缩** -> `底线`
- `## License`：**原样保留** -> `License`

## 行数预算（总上限 160）
- 首屏（标题、副文、记忆点、试用摘要）：`<=18` 行
- 闭环图 + 白话说明：`<=14` 行
- 微对比 + 短锚点 + Why 段：`<=14` 行
- `Sandtable vs Superpowers`：`<=20` 行
- 自举证明：`<=8` 行
- `Quickstart`：`<=30` 行
- 命令入口：`<=20` 行
- 目录结构 / 底线 / License：`<=18` 行

- 若预算超出，优先删减顺序：重复解释 > 目录结构细枝末节 > 命令表冗余描述 > `Quickstart` 之后的备选路径说明。
- 不允许通过压掉试用入口、对比事实锚点或闭环图来“凑行数”。

---

### 任务 T1: 重构 README 首屏叙事

**文件:**
- 修改: `README.md`

- [ ] 步骤1: 用一个更强的标题下副文和首屏摘要，明确说明 Sandtable 是什么、为谁服务、解决什么问题；主句必须写出“避免什么失控/走偏，换来什么结果”，把“给 coding agent 的方法论插件”“不是直接写代码，而是先闭环再落地”前置。
- [ ] 步骤1.2: 标题下副文、首屏主句、`Why` 段草案写完后，逐句做“就近事实锚点”检查；凡不能指向紧邻可见流程、命令、目录或文档事实的句子，一律改写或删除。
- [ ] 步骤2: 把当前分散在前文中的核心能力重新组织为更短、更能形成记忆的区块，前置“三类推演 / 异常回修正 / 持久状态与记忆 / 推演驱动的自我完善 / 自举”。
- [ ] 步骤3: 让首页前两屏形成清晰阅读路径：痛点/结果导向的一句话价值主张 → 3-5 个核心记忆点 → 1 条可行动的试用入口摘要 → 紧随其后的闭环图；首屏只允许标题、1 句副文、`<=5` 条记忆点和 1 条试用入口摘要，新增模块必须伴随压缩、合并或删除旧说明，不能只做加法。
- [ ] 步骤4: 在前两屏加入 1 条极短、事实型 `superpowers` 微对比，只点出一个已核实差异信号；把完整对比、自举证明和完整 Quickstart 展开下沉到后续 section，不能与价值主张和闭环图抢主叙事。
- [ ] 步骤4.5: 检查 `Why`、标题下副文、首屏主句、自举证明、`Quickstart` 与命令入口；凡出现新的 `superpowers` 比较句，一律删除或并回“前两屏 1 条微对比 / 正式对比区块”两处合法位置。
- [ ] 步骤5: 先按本计划的“README 骨架”写一个 section 顺序草图；若某一块放不下，只能在骨架内替换或压缩，不能额外插块。
- [ ] 步骤5.5: 首屏记忆点先写草案，再逐条执行“1 个能力 + 1 个结果”检查；若一条里并列多个方法论名词，必须拆分或删除。
- [ ] 步骤5.6: 首屏记忆点里的“结果”逐条做锚点检查：每条结果都必须能回指到 README 紧邻可见的流程、目录、命令或仓库事实；凡删去能力半句后仍像通用广告语的结果句，一律改写或删除。
- [ ] 步骤6: 验证（TC1 / TC4 / TC7 / TC8）
  运行: `rg -n "^# |^## |autopilot|三类推演|状态机|docs/sandtable|superpowers" README.md`
  预期: 首屏关键概念都可被直接定位，且前两屏主叙事集中在“为什么值得试 + 如何闭环防失控”。

### 任务 T2: 加入明确流程图与差异化对比

**文件:**
- 修改: `README.md`

- [ ] 步骤1: 在 README 中加入 GitHub 可读的流程图，明确展示从需求到验证的闭环，并显式表达 anomaly / breach / blocked 会回到修正循环；该图必须在首个滚屏内或紧接首屏下方出现。
- [ ] 步骤1.5: 给闭环图补一层白话说明：即使读者不懂 `RECON / OBJECTIVES / MENTAL / REDTEAM / IMPL` 这些术语，也能看懂“先摸清、再定目标、再计划、再预演、出问题就回修正”的闭环。
- [ ] 步骤2: 加入一个基于已核实事实的 `superpowers` 对比区块，产物格式写死为两句制逐点对比：第一句只写一个 `superpowers` 已核实事实；第二句只写 Sandtable 在本仓 `README` / 目录 / 命令中能直接指认的对应事实。两句都必须各自显式包含一个具体名词锚点。任何不能挂到该事实上的句子一律删除，不允许出现第三句点评或解释句。
- [ ] 步骤3: 对比文案只写“重心差异”和“你会更快感知到什么”，不写未经证实的 superiority claim，不攻击对方；凡出现“更强 / 更完整 / 更可靠 / 更适合 / 更系统”这类无事实锚点词，必须删改为中性事实表述。
- [ ] 步骤3.5: 全文扫描 `superpowers`；除前两屏那 1 条微对比与正式 `Sandtable vs Superpowers` 区块外，其它 section 一律不得保留新的比较句。若 `Why` 段需要表达差异，只能回指 Sandtable 自身事实，不再点名比较。
- [ ] 步骤4: 验证（TC2 / TC3）
  运行: `rg -n "superpowers|anomaly|breach|blocked|MENTAL|REDTEAM|IMPL|state|memory|autopilot" README.md`
  预期: 流程图与对比区块都存在，且关键词覆盖 Sandtable 的核心差异。
- [ ] 步骤5: 人工逐句审查对比区块，凡不是“事实引用 / 对应仓库事实”的句子一律删改；每个对比点都必须可回指到已核读的 `superpowers` README 事实，且第二句必须能回指到 Sandtable 仓库内的具体事实。凡出现第三句、评价形容词、替读者下判断的句子、或不能回指任一侧事实的句子，一律删除。

### 任务 T3: 保留试用路径并补上自举证明

**文件:**
- 修改: `README.md`

- [ ] 步骤1: 在首页中加入“这个仓库正在用 Sandtable 自己迭代自己”的表达，并用 `docs/sandtable/` 等现有事实作为支撑。
- [ ] 步骤2: 在首屏放一条明确且可行动的“立刻试用”摘要，至少说明默认推荐路径、该把哪句话发给 AI、以及先执行哪一步；并在前两屏内放一个只跳向同一 `Quickstart` 区块的锚点，不得再跳向其它“安装 / 接入”类 section。
- [ ] 步骤3: `Quickstart` 区块按固定顺序给出统一 AI 快速接入三件套：1 句可复制的安装指令、1 个安装后的接线动作说明、1 条安装后的第一条命令；README 不再拆 `Claude Code` / `Cursor` 子块。
- [ ] 步骤3.5: 若不同 harness 的最后接线动作不同，README 只保留“按 AI 的安装结果完成最后接线；若提示重载窗口/重开工作区使规则生效，就照做”，分支细节留在 `INSTALL.md`。
- [ ] 步骤3.7: `Quickstart` 必须成为唯一试用落点；不要再额外保留一个会与之竞争的独立“安装 / 接入”主 section。
- [ ] 步骤4: 对照“现有 README 逐段映射表”先做拆除计划：逐项执行当前 README 哪些段落删除、哪些并入新首屏、哪些下沉压缩；没有完成这张映射表对应动作，不要开始自由改写。
- [ ] 步骤5: 清理长段说明与低信噪比重复，把 README 维持在“成熟但不浮夸”的语气上；若新增首页版块，必须同步合并或删除旧的重复说明，并把总行数控制在 160 行以内。
- [ ] 步骤6: 改写前先按“行数预算”做 section 级分配，确认最小可行内容能放入 160 行；若预算不够，按既定删减顺序裁剪，而不是临时压掉关键入口。
- [ ] 步骤7: 验证（TC5 / TC6 / TC7 / TC8）
  运行: `rg -n "docs/sandtable|安装|接入|Quickstart|命令|/sandtable-start|/sandtable-autopilot|/sandtable-rehearse" README.md`
  预期: 自举信号、首屏可行动的 AI 安装摘要、前两屏可见的试用入口、安装入口与关键命令都仍然可见。

### 任务 T4: 文案终审与 GitHub 呈现检查

**文件:**
- 修改: `README.md`

- [ ] 步骤1: 通读 README，检查有没有失实比较、过长段落、重复观点或难以扫描的 section。
- [ ] 步骤1.5: 按文案红线清单逐句人工核对：删除无锚点优越词、替读者下判断的煽动句、以及只报抽象概念不报仓库事实的口号句；凡“删去后不影响事实理解”的句子优先删除。
- [ ] 步骤1.6: 再做一次全文 `superpowers` 位置审计：除前两屏 1 条微对比与正式对比区块外，任何 section 若再出现新的 `superpowers` 比较句，一律判失败并回删。
- [ ] 步骤2: 人工确认 Markdown 在 GitHub 首页语境下可读，流程图与表述不会要求仓外资源才能理解。
- [ ] 步骤3: 对照 `tests.md` 逐条人工验收，确保首屏、流程图、对比、自举、试用路径与长度控制六个目标都落地；特别核对“从顶部开始顺读，前两屏内只优先解决两个判断题”“流程图紧跟首屏”“除合法位置外没有新的 `superpowers` 比较句”“README 默认路径已统一成 AI 自助安装”“同一 Quickstart 落点内同时给出安装指令/接线动作/第一条命令，且不再残留失实的 marketplace 说法”。
- [ ] 步骤4: 验证（TC1-TC8）
  运行: `python - <<'PY'
from pathlib import Path
text = Path("README.md").read_text()
print("lines", len(text.splitlines()))
print("chars", len(text))
for token in ["superpowers", "docs/sandtable", "/sandtable-autopilot", "三类推演"]:
    print(token, token in text)
PY`
  预期: README 规模可控，关键 token 存在，且人工审读能确认首屏未膨胀、前两屏优先级正确、首屏有可行动试用摘要、前两屏内有试用入口，且总行数不超过 160；不能只看行数作主观判断。

## 文案红线清单
- 禁用无锚点优越词：如“更强”“更成熟”“更可靠”“更完整”“更适合”。
- 禁用替读者下判断的煽动句：如“你一看就会想试”“你会立刻感觉它更像真正的方法论”。
- 禁用只报抽象概念不报仓库事实的口号句：如“成熟、形象、差异化卖点拉满”。
- 对比区块只写两句制逐点事实，不写第三句点评。
- 除前两屏 1 条微对比与正式对比区块外，其它 section 不得新增 `superpowers` 比较句。
- 任一结果句若不能就近回指到仓库事实，视为违规文案。
- 标题下副文、首屏主句、`Why` 段若不能就近回指到仓库事实，也视为违规文案。

---

## 自查
- PRD 覆盖：FR1-FR7 都映射到 T1-T4，无遗漏。
- tests.md 覆盖：TC1/TC4/TC7 由 T1/T4 验证；TC2/TC3 由 T2/T4 验证；TC5/TC6/TC8 由 T3/T4 验证。
- 范围：只改 `README.md` 与本 feature 文档，不修改方法论内容本身。
- MUST NOT：不引入无依据数据、不虚构 `superpowers` 缺失能力、不做无关功能改动。
