# MENTAL_REHEARSAL 轮 1 · README 首页重塑

## 结论
`ANOMALY_FOUND`

本轮 3 个只读子 agent 全部完成，但至少 1 个返回 anomaly，且主 agent 已核实成立；因此本轮不计入 `autonomy.completed_rounds.mental`。

## 已核实异常

1. **前两屏优先级不够硬**
   - 表现：原 PRD / plan 只要求“前两屏讲清价值与闭环”，但没有把前两屏限定为“为什么值得试 + 如何闭环防失控”这两个判断题，也没有限定流程图必须紧跟首屏。
   - 风险：实现时容易把价值主张、能力清单、对比、自举、Quickstart 同时前置，导致前两屏互相稀释。
   - 处置：已在 `prd.md` / `tests.md` / `plan.md` 中加入前两屏优先级、首屏预算与流程图位置硬约束。

2. **试用入口与长度预算不够硬**
   - 表现：原文只要求“保留安装/接入/命令入口”，但没有要求前两屏内单跳可达，也没有要求新增模块必须伴随删改旧说明。
   - 风险：首页可能更好看，但变长、变空、入口后移。
   - 处置：已在 `prd.md` / `tests.md` / `plan.md` 中加入前两屏试用入口、长度预算和 `TC8`。

3. **`superpowers` 对比边界仍可能滑向无证优越性判断**
   - 表现：原约束只禁止“虚构对方没有的能力”，但没有一律禁止“更强 / 更完整 / 更可靠 / 更适合 / 更系统”等无事实锚点的 superiority claim。
   - 风险：实现者可能写出不直接造假、但仍越界的宣传式对比。
   - 处置：已在 `prd.md` / `tests.md` / `plan.md` 中把对比收紧为“已核实事实 + 重心差异 + 读者先感知到什么”，并要求人工逐句审查。

## 主 agent 裁决
- 本轮定性为文档级 anomaly，已完成核实与回写。
- `state.md.phase` 保持 `MENTAL_REHEARSAL`。
- 下一步：按修正后的文档重跑第 1 轮 mental，直到 3 个只读子 agent 全部返回 `LOGIC_CLOSED`。
