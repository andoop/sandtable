# 待开发者澄清 · Questions

> 仅放“读代码、读文档都无法确定，必须开发者拍板”的阻塞性问题。
> 有未解决的问题时，`state.md` 的 `blocked` 应为 `true`。

当前无阻塞未决问题。

已确认范围：
- 首期只支持中文与英文两种安装语言。
- 语言由 AI 读取用户实际贴过去的话自行判断，不要求额外语言 flag。
- 语言跟随规则作用于安装流程和放到用户本地的文档，且**包含 `skills/`**。
- Codex / Kiro 只通过通用安装路径支持，不要求新增专属接线机制。
