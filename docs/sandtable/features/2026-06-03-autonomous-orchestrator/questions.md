# 待开发者澄清 · Questions

> 仅放“读代码、读文档都无法确定，必须开发者拍板”的阻塞性问题。
> 有未解决的问题时，`state.md` 的 `blocked` 应为 `true`。

当前无阻塞问题。

若后续实现或推演中发现以下情形，再追加新条目：
- 自动模式是否允许在未再询问开发者的前提下直接执行 `INTEGRATE`；
- 某步需要人工登录、授权、审批或外部系统权限；
- 某个产品意图无法从需求原文、代码与文档中确认。
