# 头脑预演报告 · 轮 7

结果：`LOGIC_CLOSED`

## 背景
- 在第 6 轮后，剩余风险已收敛为单一问题：`TC8 / T4 / T5 / T6` 的验收口径仍可能出现“关键词命中即通过”的假阳性。
- 本轮继续执行 `/sandtable-mental`，仅围绕这一组验收命令与文案收束做最小范围修补与复核。

## 本轮核实与修补
- 已核实第 6 轮遗留的几类问题确实成立：
  - `TC8`、`T5`、`T6` 一度没有完全同口径；
  - 负向检查原先用裸 `rg`，与“0 命中才通过”的语义相反；
  - `rehearse` 的索引边界一度只要求弱匹配，未收紧到完整短语；
  - `串起三类推演与复盘` 这类旧表述曾有漏网窗口。
- 已继续修订 `tests.md` / `plan.md`：
  1. 把负向检查统一改成 `! rg`，并在预期里明确“0 命中”才算通过；
  2. 把 `README/AGENTS/rule` 对 `/sandtable-rehearse` 的边界断言收紧为完整短语 `只串推演与复盘`；
  3. 为命令正文补入单独的 `rg` 断言，避免只验证 frontmatter 而遗漏正文；
  4. 让 `T6` 明确“先复跑与 `TC8/T5` 同口径的完整终验，再补跑全仓 smoke”；
  5. 把 `T4` 明确降级为“TC8 局部预检”，避免与完整终验混淆；
  6. 补齐 `串起三类推演与复盘` 等旧表述的负向词表。

## 终检结论
- 并行只读子 agent 最终返回 `LOGIC_CLOSED`，且主 agent 已抽查通过：
  - `TC8.When`、`T5`、`T6` 现在在“正向检查 / 负向 0 命中 / 文件存在性 / rehearse 正文”这四层上已对齐；
  - `T4` 的定位已收束为局部预检，不再冒充完整 `TC8`；
  - `前五步/前序编排`、`只串推演与复盘`、frontmatter/正文三套字面已各司其职；
  - 未再发现新的计划级 anomaly。

## 结论
- 本需求的头脑预演阶段现已闭环，可以进入下一阶段：
  - `/sandtable-redteam` 做红蓝对抗，或
  - `/sandtable-live` 做实现预演。
